---
// Site root: redirect visitors to the public "Coming Soon" splash.
// (On the getready/admin standalone Workers the root is rewritten earlier in
// src/middleware.ts, so this redirect only applies to the main site.)
export const prerender = false;

return Astro.redirect("/coming-soon", 302);
---
